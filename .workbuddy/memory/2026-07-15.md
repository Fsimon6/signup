# 2026-07-15

## sf_click.py 严格流程重写
- 按用户提供的详细操作流程完全重写 `sf_click.py`：
  - 目标字段从"扣款/优惠券返款"改为 **"交易金额（元）"**（4 种提取模式）
  - 粘贴单号 → Tab 跳转到"查询"按钮 → Enter 点击
  - 新增横向滚动逻辑：End 键 / Shift+滚轮 / Right 键，找"交易金额（元）"列
  - 失败单号记录到 `.sf_failed.json`
  - 移除所有扣款/返款差额计算逻辑

## sf_click.py 改为控制台 JS 注入
- pyautogui 坐标点击无法定位顺丰输入框，改用浏览器控制台执行 JS 直接操作 DOM
- 输入单号：`document.querySelector('#searchForm_waybillNo')` + React 原生 setter 赋值
- 点击查询：`document.querySelector('button.search-button').click()`
- 金额提取：DOM 表头「交易金额（元）」→ 同列数据行值；找不到时滚动表格再试
- `copy()` 写回剪贴板，Python 读取后回填飞书

## 控制台修复（Ctrl+Shift+J 开关 bug）
- 根因：Ctrl+Shift+J 是 toggle，第二次调用时关掉了控制台
- 修复：加 `_console_opened` 标记，只在第一次打开控制台
- 但仍有 Chromium "allow pasting" 安全警告阻止粘贴

## 切换为地址栏 javascript: 注入（v3）
- 彻底放弃控制台方案，改用地址栏 `javascript:` 协议
- `Ctrl+L` 聚焦地址栏 → 逐字键入 `javascript:`（避免被 Chromium 剥离）→ 粘贴 JS 体 → Enter
- 输入+查询合并为一条 `javascript:void(...)` 调用
- 金额提取改用 `Ctrl+A Ctrl+C` 全页复制 + 正则 `交易金额[（(]元[）)]\s*([\d,]+\\.?\\d*)` 匹配
- 不需要任何 DevTools 交互，彻底避开 "allow pasting" 问题

